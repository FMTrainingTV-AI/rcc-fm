# Intents

One per stream of work, written by the `discovery` skill (pm plugin) —
problem, proposed outcome, constraints, open questions, size call. Shared
record: the next stage (`/wayfinder`, plan mode) reads it. See the pm
plugin's `SDLC.md`.
