# Decisions

Architecture/decision records, one file per durable choice retrievable by
topic. Matt Pocock's `/domain-modeling` owns the format. Superseded decisions
get `status: superseded` and a link both ways — archive, don't delete.
